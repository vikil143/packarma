import React from 'react';
import {StyleSheet, TextInput} from 'react-native';

export default function TextBox() {
  return <TextInput style={[styles.textInput]} value={{}} />;
}

const styles = StyleSheet.create({
  textInput: {
    backgroundColor,
  },
});
